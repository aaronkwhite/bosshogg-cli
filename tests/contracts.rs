//! JSON contract / schema-shape tests.
//! Separated from cli.rs because these tests focus on output-schema stability
//! rather than command behavior.

mod common;

mod error_mapping;
mod json_contract;
mod rest_shapes;
