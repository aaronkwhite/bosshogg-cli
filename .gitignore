# Rust
/target
Cargo.lock.bak
**/*.rs.bk

# Cargo.lock IS committed — bosshogg is a binary crate, so lockfile in VCS is
# the recommended practice (https://doc.rust-lang.org/cargo/faq.html).

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Local config and secrets
.env
.env.local
/config.toml
**/local_*

# Background task / agent output (when run in this repo)
/tmp/
*.log
.claude/scheduled_tasks.lock

# macOS
.AppleDouble
.LSOverride

# Generated docs (if ever added)
/book/

# Internal process/dev docs (plans, dogfood runs, session handoffs, specs)
docs/superpowers/
